<odoo>
	
	<record id="product_template_form_view_inherit_subscription" model="ir.ui.view">
	        <field name="name">product.template.form.view.inherit.subscription</field>
	        <field name="model">product.template</field>
	        <field name="inherit_id" ref="product.product_template_only_form_view"/>
	        <field name="arch" type="xml">
	
	            <xpath expr="//field[@name='barcode']" position="after">
	                <field name="subscription_invoice" />
	            </xpath>
	
	        </field>
	    </record>
	
	
	
	<record id="action_product_template_subscription_view" model="ir.actions.act_window">
            <field name="name">Subscription Product</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">product.template</field>
            <field name="view_mode">kanban,form,tree,activity</field>
			<field name="domain">[('subscription_invoice', '=', True)]</field>
			<field name="context">{'default_subscription_invoice': True}</field>
            <field name="view_id" eval="False"/> <!-- Force empty -->
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a new Subscription Product
              </p><p>
                You must define a product for Subscription.
              </p>
            </field>
        </record>

    <menuitem id="subscription_products_menu" name="Products" parent="subscription_agreement_menu"
			action="action_product_template_subscription_view" sequence="2" />
</odoo>
